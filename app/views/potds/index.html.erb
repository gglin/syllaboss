<%= render partial: "layouts/header_index", locals: {chosen_materials: @potds} %>

<% @material_columns = ["Wikipedia", "Presentation URL"] %>

<%= render partial: "layouts/body_materials_index", locals: {materials: @potds, material_columns: @material_columns } %>