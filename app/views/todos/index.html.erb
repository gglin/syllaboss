<%= render partial: "layouts/header_index", locals: {chosen_materials: @todos} %>

<% @material_columns = [{name: "URL", data_type: "data-sort=string"}] %>

<%= render partial: "layouts/body_materials_index", locals: {materials: @todos, material_columns: @material_columns } %>

<% if @deleted_from_preview %>
  <script>
    parent.hideFrameAndReload();
  </script>
<% end %>